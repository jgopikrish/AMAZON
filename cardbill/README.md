# Welcome to Tango-Controls server cardbill


Clone this repo and build single executable jar using maven:

```bash
$> mvn package
```

Run the server:

```bash
$> java -jar target/cardbill-1.0-SNAPSHOT.jar

```